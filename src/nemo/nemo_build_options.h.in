//
// Created by Mark Plagge on 7/26/20.
//


#pragma once

#cmakedefine01 NeMo_DEBUG
#cmakedefine  NOS_MAX_TIME @NOS_MAX_TIME@


#if NeMo_DEBUG
#define DEBUG NeMo_Debug
#endif
#define NEMO_DEBUG() @NeMo_DEBUG@

struct BuildOptions {
	/**@remark This technique is much cleaner but requires gcc > 7 or clang, but XL_C is not supported - Waiting for C++14/17! */
	constexpr static bool debug_mode = static_cast<bool>(NeMo_DEBUG);
	constexpr static unsigned int nos_max_time = static_cast<unsigned int>(NOS_MAX_TIME);
};
constexpr BuildOptions build_options;

